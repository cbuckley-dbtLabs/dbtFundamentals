version: 2

sources:
  - name: stripe
    database: raw
    schema: stripe
    tables:
      - name: payment
        # loaded_at_field: _batched_at
        config:
          freshness:
            warn_after: {count: 730, period: day}
            error_after: {count: 1000, period: day}
          loaded_at_field: "convert_timezone('UTC', _batched_at)"
